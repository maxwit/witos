dir-y += sysconf
dir-$(CONFIG_FLASH) += flash
dir-y += memory
dir-$(CONFIG_NET) += net
dir-y += serial
dir-y += boot
dir-y += shell
dir-y += misc
dir-$(CONFIG_MMC) += mmc
dir-$(CONFIG_GRAPHICS) += display
