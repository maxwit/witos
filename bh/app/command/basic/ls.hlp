description:
  List information about the FILEs

Usage:
  ls [OPTION]... [FILE]...

options:
  <volume> Optional parameters, the HOME volume by default
  -l [FILE]
   list detail information of the FILE (the current directory by default), if the FILE is a device, the infomation of the device will be list
  -h
   human readable
