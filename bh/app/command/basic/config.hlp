description:
  g-bios system configuration utility.

usage:
  config [options] [<attribute> [<value>]]

options:
    -a <attr> <value>     adds a new attribue with value
    -r <attr>             removes the attribute
    -g <attr>             get value
    -l                    list all
    -h                    this help
