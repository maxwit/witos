description:
  dynamic host configuration protocol client.

usage:
  dhclient [<options>]

options:
  -s
   update the server address in system configuration to the DHCP server.
  -x <NIC>
   specify the network interface.
